
# ==========================================
# API 基礎配置
# ==========================================

# API 基礎 URL - 定義所有 API 請求的根路徑
# 開發環境: http://localhost:3000
# 正式環境: https://api.yourdomain.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# 專案名稱 - 用於 API 請求中的 project 參數，區分不同專案
# 範例: minmax2025, project-a, client-website
NEXT_PUBLIC_PROJECT_NAME=minmax2025

# 預設語系 - 當無法偵測使用者語系時的備用語系
# 支援格式: zh-TW, zh-CN, en, ja, ko 等
NEXT_PUBLIC_DEFAULT_LOCALE=zh-TW

# ==========================================
# Mock API 開關
# ==========================================

# Mock API 開關 - 控制是否使用本地 Mock API 而非真實後端
# true: 使用 /api/ssr/* 路徑下的 Mock API (開發階段)
# false: 使用真實的後端 API (正式環境)
USE_MOCK_API=true

# ==========================================
# 國際化功能配置
# ==========================================

# 國際化功能總開關 - 控制整個國際化系統的啟用狀態
# true: 啟用語系偵測、國家判斷、多語系等功能
# false: 停用所有國際化功能，使用單一語系
INTERNATIONALIZATION_ENABLED=true

# 國際化資料快取策略 - 決定語系、選單等資料的快取方式
# "memory": 記憶體快取，適用於單伺服器部署
# "redis": Redis 快取，適用於多伺服器部署
# "none": 不快取，每次都呼叫 API (僅開發用)
I18N_CACHE_STRATEGY=memory

# 地理位置偵測策略 - 決定如何偵測使用者所在國家
# "cdn-only": 僅使用 CDN 提供的地理標頭 (Vercel, Cloudflare)
# "api-only": 僅使用外部 API 服務 (geoPlugin 等)
# "cdn-fallback": CDN 優先，失敗時使用 API 備援
GEO_DETECTION_STRATEGY=api-only

# 國家導引模式 - 決定偵測到使用者國家後的處理方式
# "off": 停用國家判斷與導引
# "suggest": 僅提供建議，不自動跳轉 (適合大型電商)
# "force": 自動重導向至對應國家子站 (適合跨國企業)
GEO_REDIRECT_MODE=suggest

# ==========================================
# 快取系統配置
# ==========================================

# 快取系統總開關 - 控制整個快取機制的啟用狀態
# true: 啟用頁面、模組、個人化等各層快取
# false: 停用所有快取，每次都重新產生內容
CACHE_ENABLED=true

# CDN 快取協作 - 控制是否與 CDN 協作快取
# true: 設定適當的 Cache-Control 標頭供 CDN 使用
# false: 設定 private 標頭，避免 CDN 快取
CACHE_CDN_ENABLED=false

# 會員功能開關 - 控制會員相關功能的啟用狀態
# true: 啟用會員登入、權限控制、個人化內容等
# false: 停用會員功能，所有內容為公開訪問
FEATURES_MEMBERSHIP_ENABLED=false

# ==========================================
# 額外配置 (可選)
# ==========================================

# 開發模式設定
# NODE_ENV=development

# API 請求超時設定 (毫秒)
# API_TIMEOUT=5000

# 地理位置 API 超時設定 (毫秒)
# GEO_API_TIMEOUT=800

# 快取預設 TTL (秒)
# CACHE_DEFAULT_TTL=3600

# 是否啟用 API 請求日誌
# API_LOGGING_ENABLED=true

# 是否啟用效能監控
# PERFORMANCE_MONITORING=false